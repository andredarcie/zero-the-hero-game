extends Camera2D

@onready var player := get_parent() as Player

func _ready():
	#global_position = Vector2(player.global_position.x - (320 / 2),
	#   					  player.global_position.y - (240 / 2))
	pass
